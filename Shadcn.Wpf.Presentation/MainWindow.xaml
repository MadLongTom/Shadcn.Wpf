<controls:ShadcnWindow x:Class="Shadcn.Wpf.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Shadcn.Wpf.Controls;assembly=Shadcn.Wpf" 
        xmlns:viewmodels="clr-namespace:Shadcn.Wpf.Presentation.ViewModels" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewmodels:MainWindowViewModel}"
        Title="Shadcn.Wpf Demo" Height="900" Width="1400"
        ShowTitleBar="True"
        ShowSystemButtons="True"
        ShowIcon="True"
        EnableDropShadow="True"
        EnableAnimations="True"
        WindowStartupLocation="CenterScreen">
    
    <controls:ShadcnWindow.Icon>
        <DrawingImage>
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Geometry="{StaticResource MainWindowIcon}">
                        <GeometryDrawing.Pen>
                            <Pen Brush="{DynamicResource ForegroundBrush}" Thickness="1.5"/>
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
    </controls:ShadcnWindow.Icon>

    <Window.Resources>
        <Style x:Key="StatusBarStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
            <Setter Property="Padding" Value="20,8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Navigation Menu -->
        <controls:ShadcnNavMenu x:Name="MainNavMenu"
                              Grid.Row="0"
                              Grid.Column="0"
                              ItemsSource="{Binding NavigationItems}"
                              NavigationItemSelectedCommand="{Binding NavigationItemSelectedCommand}"
                              ExpandedWidth="280"
                              CollapsedWidth="58">
            <controls:ShadcnNavMenu.Header>
                <controls:ShadcnTextBlock Text="Shadcn.Wpf" 
                                        TextSize="Large" 
                                        IsEmphasized="True"/>
            </controls:ShadcnNavMenu.Header>
        </controls:ShadcnNavMenu>

        <!-- Main Content Area -->
        <Frame x:Name="MainContentFrame"
               Grid.Row="0"
               Grid.Column="1"
               Style="{StaticResource AnimatedFrameStyle}"
               Background="{DynamicResource BackgroundBrush}"/>

        <!-- Status Bar -->
        <Border Grid.Row="1" 
                Grid.ColumnSpan="2"
                Style="{StaticResource StatusBarStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <controls:ShadcnTextBlock Text="{Binding StatusMessage}"
                                            VerticalAlignment="Center"/>
                    <controls:ShadcnTextBlock Text=" | Press F12 to open new ShadcnWindow"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource MutedForegroundBrush}" />
                </StackPanel>

                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <controls:ShadcnButton Content="Toggle Theme" 
                                         Variant="Ghost" 
                                         Size="Small"
                                         Command="{Binding ToggleThemeCommand}"
                                         Margin="0,0,8,0"/>
                    <controls:ShadcnTextBlock Text="{Binding CurrentTheme, StringFormat='Theme: {0}'}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</controls:ShadcnWindow>
